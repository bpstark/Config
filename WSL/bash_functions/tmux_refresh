function refresh {                                                                                
    if [ -n "$TMUX" ]; then                                                                               
        export $(tmux show-environment | grep "^DISPLAY")                                             
    fi
}                                                                                                 
